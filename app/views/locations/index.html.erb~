<h1>Locations</h1>

<table class="table">

<% @locations.each do |location| %>
  <tr>
    <td><%= image_tag(location.image_url) %></td>
    <td><%= location.title %></td>
    <td><%= sanitize(location.description) %></td>
    <td><%= @location.lines %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Location', new_location_path %>
